apiVersion: v1
kind: Service
metadata:
  name: {{ include "msa-shop.fullname" . }}
  labels:
    {{- include "msa-shop.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "msa-shop.selectorLabels" . | nindent 4 }}
