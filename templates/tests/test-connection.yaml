apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "msa-shop.fullname" . }}-test-connection"
  labels:
    {{- include "msa-shop.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "msa-shop.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
